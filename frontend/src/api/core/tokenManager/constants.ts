/**
 * Token Manager Constants
 * Central location for all token-related constants
 */

// Constants for token keys to ensure consistency
export const TOKEN_KEYS = {
  AUTH_TOKEN: 'authToken',
  REFRESH_TOKEN: 'refresh_token',
  USER: 'auth_user'
} as const;
