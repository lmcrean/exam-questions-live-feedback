# ============================================
# PROJECT CONFIGURATION
# ============================================
# Copy this file to .env and fill in your values
# This file contains project-level configuration that can be reused when cloning the monorepo

# Project Identity
PROJECT_ID=product-one-477118
APP_NAME=dottie
APP_DISPLAY_NAME=Dottie

# Firebase Configuration
# IMPORTANT: This is the single source of truth for all Firebase/web URLs
# All URLs are auto-derived from this project ID:
# - Main URL: https://{FIREBASE_PROJECT_ID}.web.app
# - Preview URLs: https://{FIREBASE_PROJECT_ID}--{channel-id}.web.app
# - CORS Origins: Automatically configured based on this value
FIREBASE_PROJECT_ID=product-one-477118

# GCP Configuration
GCP_PROJECT_ID=product-one-477118
GCP_LOCATION=us-central1
GCP_SERVICE_ACCOUNT=github-actions-cloudrun@product-one-477118.iam.gserviceaccount.com
GCP_ARTIFACT_REPOSITORY=dottie-api-images

# Note: Web URLs and CORS origins are auto-derived from FIREBASE_PROJECT_ID
# No need to configure them separately!

# Cloud Run Configuration
CLOUD_RUN_SERVICE_NAME_PATTERN=dottie-api-{branch}
CLOUD_RUN_REGION=us-central1

# ============================================
# APPLICATION SECRETS (Shared across all apps)
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development
LOCAL_DEV=true

# JWT Secrets for authentication (GENERATE NEW ONES!)
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-here
REFRESH_SECRET=your-refresh-secret-here

# Google Gemini API (RECOMMENDED - Best Free Tier!)
# Get FREE API key at: https://ai.google.dev/
# 45,000 requests/month FREE
GEMINI_API_KEY=your-gemini-api-key
GEMINI_DAILY_LIMIT=1333

# Hugging Face API (Backup option)
# Get token at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_TOKEN=

# Vertex AI Configuration
GOOGLE_APPLICATION_CREDENTIALS=./gcp-setup/gcp-sa-key.json

# Database Configuration
DB_TYPE=supabase
NEON_DATABASE_URL=your-database-url

# Auth Provider (Stack Auth)
VITE_STACK_PROJECT_ID=your-stack-project-id
VITE_STACK_PUBLISHABLE_CLIENT_KEY=your-stack-publishable-key
STACK_SECRET_SERVER_KEY=your-stack-secret-key

# Redis Configuration (Required for Workers App)
# Development: docker run -d -p 6379:6379 redis
# Production: Redis Cloud (free tier) or Upstash (~$15/month)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Update all values with your project-specific information
# 3. Generate new JWT secrets (see command above)
# 4. Add your API keys and database credentials
# 5. For GitHub CI/CD, add these as secrets in your repository
# 6. Run ./setup-project.sh to auto-configure Firebase and other configs
